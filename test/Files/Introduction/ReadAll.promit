/**
 * Example of 'File.read_all' method.
 *
 * Part of Promit Test Suit, location /test/Files/Introduction/ReadAll.promit
 *
 * Created by SD Asif Hossein in 28th June, 2022 (Tuesday) at 12:48 AM (UTC +06).
 */

// Use 'File.read_all()' to read the whole file at once.

# Note : 'File.read_all()' does not move the cursor.

take file = File('common_file.txt', File::MODE_READ);

// Now read the whole file.

showl file.read_all();

// Close the file.

file.close();
