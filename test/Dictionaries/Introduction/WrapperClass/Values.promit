/**
 * Demonsration of 'Dictionary.values' method
 *
 * Part of Promit Test Suit, location /test/Dictionary/Introduction/WrapperClass/Values.promit
 *
 * Created by SD Asif Hossein in 29th June, 2022 (Wednesday) at 01:40 AM (UTC +06).
 */

// 'Dictionary.values()' returns all the values within the dictionary as a 
// list, paired with a specific key. It's kind of like 'Dictionary.keys()', 
// with difference that it will return the values instead of the keys.

take dict = {
	'Company' : 'BMW',
	'Model'   : 'M3 GTR',
	'Mileage' : 55.55
};

showl "The values of 'dict' dictionary are : ${dict.values().join(', ')}";
