/**
 * Demonsration adding constant properties while
 * constructing dictionaries.
 *
 * Part of Promit Test Suit, location /test/Dictionary/Introduction/Dictionary.promit
 *
 * Created by SD Asif Hossein in 29th June, 2022 (Tuesday) at 01:15 AM (UTC +06).
 */

// Use '(const)' with the paranthesis after the key
// name to make it a constant property.

take dict = {
	'name'(const) : 'SD Asif Hossein'
};

// Read more about constant properties at Properties section.

showl is_const_prop(dict, 'name');    // Output : true
