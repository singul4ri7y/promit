/**
 * Demostration of comparison of NaN value.
 *
 * Part of Promit Test Suit, location /test/Numbers/Introduction/NaNIsIncomparable.promit
 *
 * Created by SD Asif Hossein in 25th June, 2022 (Saturday) at 04:06 PM (UTC +06).
 */

// The value NaN is incomparable.
// Keep that in mind that, while using 'NaN'
// everything you do, if it's comparison the result
// will be false and if it's operation/calculation
// the result will be always NaN.

showl 'NaN < 0 : ${NaN < 0}';    // Output : NaN < 0 : false
showl 'NaN > 0 : ${NaN > 0}';    // Output : NaN > 0 : false
showl 'NaN == 0 : ${NaN == 0}';    // Output : NaN == 0 : false
showl 'NaN * 0 : ${NaN * 0}';    // Output : NaN * 0 : NaN
showl 'Infinity / NaN : ${Infinity / NaN}';    // Output : Infinity / NaN : NaN

