/**
 * Demonstration of 'List.reverse' method.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/Reverse.promit.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 02:42 AM (UTC +06).
 */

// 'List.reverse()' returns a new list, reversed, without
// affecting current list.

take list = [1, 2, 3, 4, 5];

showl 'The list we are using : ${list.join(', ')}';
showl 'After reversing the list : ${list.reverse().join(', ')}';
