/**
 * Demonstration of 'List.remove' method.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/Remove.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 03:46 AM (UTC +06).
 */

// 'List.remove(index, range)' removes values from a list starting
// from 'index' upto 'range' and returns the elements as a 
// seperate array.

take list = List::range(51, 1);

showl 'The list we are using : ${list.join(', ')}';
show 'After removing, from index 6 to range 17 : ';

take rest = list.remove(6, 17);

showl list.join(', ');

showl 'Removed elements are : ${rest.join(', ')}';
