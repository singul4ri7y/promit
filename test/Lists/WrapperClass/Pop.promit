/**
 * Demonstration of 'List.pop' method.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/Pop.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 12:48 AM (UTC +06).
 */

// 'List.pop' pops out the very last element
// of the list. The list acts as a stack.

take list = [ 1, 2, 3, 4, 5 ];

showl 'The list we are using : ${list.join(', ')}';
showl 'The last element after shift : ${list.pop()}';
showl 'The list now : ${list.join(', ')}';
