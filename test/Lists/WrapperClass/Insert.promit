/**
 * Demonstration of 'List.insert'.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/Insert.promit
 *
 * Created by SD Asif Hossein in 29th June, 2022 (Wednesday) at 03:38 PM (UTC +06).
 */

// Use 'List.insert(elements...)' to insert elements at the end
// to a list.
// Returns the length of the list after the insertion is done.

take list = [];

for(take i = 1; i <= 5; i++) 
    list.insert(i);

showl list;    // Output : [ 1, 2, 3, 4, 5 ]
