/**
 * Demonstration of 'List.map' method.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/Map.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 12:11 AM (UTC +06).
 */

// 'List.map(mapper)' maps all the elements of a list
// through a mapper function and returns a new formed list.

// 'mapper' function will get passed by current_element
// current_index and current_list respectively.

take list = [101, 202, 303, 404, 505];

showl list.map(fn _(elem) {
    return elem * 2;
}).join(', ');    // Output : 202, 404, 606, 808, 1010
