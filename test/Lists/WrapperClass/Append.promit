/**
 * Demonstration of 'List.append' method.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/Append.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 12:35 AM (UTC +06).
 */

// The 'List.append(lists...)' returns a new list appending 
// the current list and all the provided lists.

# Note : Current list (on which it is applied on) stays unaffected.

take list = [1, 2, 3];

showl list.append(['Strin', 'gs'], [Time(), Time().get_seconds()], [{ a : 100, b : 200}]);
showl 'But the actual list stays unaffected...';
showl list;
