/**
 * Demonstration of 'List.at' method.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/At.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 12:54 AM (UTC +06).
 */

// 'List.at(index)' returns the element associated with that index
// similarly, like '[index]'.

# Note : Only difference between getting an element using 'List.at'
#        and '[i]' is 'List.at' will not trigger any error if list
#        index is out of bound, instead it will return the first/last
#        element of the list.

take list = [1, 2, 3, 4, 5];

showl 'The list we are using : ${list.join(', ')}';
showl 'Getting element of index 234523634  : ${list.at(234523634)}';     // Output : 5
showl 'Getting element of index -882735007 : ${list.at(-882735007)}';    // Output : 1
