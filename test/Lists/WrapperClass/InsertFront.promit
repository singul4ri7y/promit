/**
 * Demonstration of 'List.insert_front'.
 *
 * Part of Promit Test Suit, location /test/Lists/Introduction/WrapperClass/InsertFront.promit
 *
 * Created by SD Asif Hossein in 29th June, 2022 (Wednesday) at 03:43 PM (UTC +06).
 */

// Use 'List.insert_front(elements...)' to insert elements to the front
// of a list.
// Returns the length of a list after insertion.

take list = [];

for(take i = 1; i <= 5; i++)
	list.insert_front(i);

showl 'List length after insertion : ${list.insert_front('A', 'B', 'C')}';    // Output : List length after insertion : 8
showl list;    // Output : [ 'A', 'B', 'C', 5, 4, 3, 2, 1 ]
