/**
 * Demonstration of string immutability in Promit.
 *
 * Part of Promit Test Suit, location /test/Strings/WrongSyntax/StringsAreImmutable.promit
 *
 * Created by SD Asif Hossein in 30th June, 2022 (Thursday) at 04:51 PM (UTC +06).
 */

// Strings are immutable in Promit, due to string interning.

take string = 'This is a string!!';

showl string[1];    // Output : h

string[1] = 'o';    // Error : Attempt to set a property to a non-instance/non-dictionary value!
