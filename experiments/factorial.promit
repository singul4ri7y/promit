// A factorial promit program written by SD Asif Hossein.

show "Enter the number: ";

take factorial = recieve(num);

show 'The factorial of $factorial is: ';

for(take i = factorial - 1; i > 0; i--) 
	factorial *= i;

showl factorial;