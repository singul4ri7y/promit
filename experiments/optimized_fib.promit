// A promit program of optimized fibonacci identifier.

// Dynamic programming tabulation approach.

fn fib(n, tab) {
	if(n < len(tab)) 
		return tab[n];
	
	tab[n] = fib(n - 1, tab) + fib(n - 2, tab);
	
	return tab[n];
}

take tab = [0, 1, 1];

showl fib(40, tab);