// Program to rename a file.

show 'Enter the file name to be renamed: ';

take fileName = recieve;

show 'Enter the new file name: ';

if(System::rename(fileName, recieve)) 
	showl 'File successfully renamed!';
else showl 'Failed to rename the file!';
